"""
intake/
Block 1 — Site Intake & Data Builder.

Public API (available after all steps are implemented):
  build_site_diagnostic(form, base_dir) → site_diagnostic dict
  generate_profiles(sdi, base_dir)      → profiles dict (also saves cache)
  load_profiles(sdi, base_dir)          → profiles dict (from cache)
"""
